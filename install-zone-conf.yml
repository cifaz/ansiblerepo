# 配置文件处理
- hosts:
    - localhost
  vars_files:
    - vars.yml
  tasks:
    - name: conf nginx
      template:
        src: conf/nginx.conf.j2
        dest: "/etc/nginx/conf.d/{{ item.server_name }}.conf"
      loop: "{{ nginx_conf }}"


