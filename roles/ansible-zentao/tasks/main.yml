#

- name: download zentao
  get_url:
    url: "{{download_url}}"
    dest: "{{download_dir}}/zentao.rpm"
    force: no
    state:
      - directory

- name: rpm install zentao
  yum:
    name: "{{download_dir}}/zentao.rpm"
    state: present