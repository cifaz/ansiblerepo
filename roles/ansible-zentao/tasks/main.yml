#

- name: download zentao
  get_url:
    url: "{{download_url}}"
    dest: "{{download_dir}}/zentao.tar.gz"
    force: no

- name: rpm install zentao
  shell:  "tar -zxvf  {{download_dir}}/zentao.tar.gz -C /opt"

- name: start zentao
  shell: /opt/zbox/zbox start
