# add_host 是否可以替换hosts?

- hosts:
    - localhost
  vars_files:
    - vars.yml
  tasks:
    - name: test..
      debug:
        msg: "{{ item }}"
      with_items: "{{ cifaz_roles }}"
    - name: "import {{ item }}"
      import_role:
        name: "{{ item }}"
      vars_files:
          - vars.yml
      with_items: "{{ cifaz_roles }}"
#    - name: import ansible-xwiki
#      import_role:
#        name: ansible-xwiki
#      vars_files:
#          - vars.yml
#  roles:
#    - role: "{{ item }}"
#      vars_files:
#        - vars.yml
#      with_items:
#        - srsp.oracle-java
#        - bertvv.dnsmasq

