# add_host 是否可以替换hosts?

- hosts:
    - localhost
  vars_files:
    - vars.yml
  tasks:
    - name: test..
      debug:
        msg: "{{ item }}"
      with_items: "{{ cifaz_roles }}"
    - name: import srsp.oracle-java
      import_role:
        name: srsp.oracle-java
      vars_files:
          - vars.yml
#  roles:
#    - role: "{{ item }}"
#      vars_files:
#        - vars.yml
#      with_items:
#        - srsp.oracle-java
#        - bertvv.dnsmasq

