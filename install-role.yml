- hosts:
    - localhost
  vars_files:
    - vars.yml
  tasks:
    - name: test..
      debug:
        msg: "{{item}}"
      with_items: "{{cifaz_roles}}"
  roles:
    - role: "{{item}}"
      vars_files:
        - vars.yml
      with_items: "{{cifaz_roles}}"

